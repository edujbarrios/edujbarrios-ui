const overrides = [{ person: "M. Chen", change: "Blocked → Approved", reason: "Verified exception" }, { person: "A. Silva", change: "Auto → Review", reason: "Low source coverage" }, { person: "R. Patel", change: "v22 → v21", reason: "Quality regression" }];
export function HumanOverrideLedger() { return <section className="w-full max-w-md rounded-xl border border-white/12 bg-[#0b0f14]/92 p-4 shadow-2xl"><header className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-[#fb923c]">Override ledger</p><h3 className="mt-1 text-base font-bold text-white">Human decisions</h3></div><span className="text-[0.65rem] font-semibold text-slate-500">Last 24 hours</span></header><ol className="mt-4 space-y-2">{overrides.map((item, index) => <li key={item.person + item.change} className="grid grid-cols-[auto_1fr] gap-3 rounded-lg border border-white/9 p-3"><span className="grid h-8 w-8 place-items-center rounded-full bg-[#fb923c]/12 text-xs font-bold text-[#fdba74]">{index + 1}</span><div><div className="flex justify-between gap-2"><p className="text-xs font-semibold text-slate-200">{item.change}</p><span className="text-[0.65rem] text-slate-500">{item.person}</span></div><p className="mt-1 text-[0.65rem] text-slate-500">{item.reason}</p></div></li>)}</ol><p className="mt-3 text-[0.65rem] text-slate-500">All overrides are immutable and exportable.</p></section>; }
